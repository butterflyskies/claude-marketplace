{
  "name": "memory",
  "description": "MCP wiring for memory-mcp semantic memory server",
  "author": {
    "name": "butterflyskies"
  },
  "license": "MIT OR Apache-2.0",
  "userConfig": {
    "memory_url": {
      "type": "string",
      "title": "Memory MCP URL",
      "description": "HTTP endpoint for your memory-mcp server (e.g. https://memory-mcp.example.com/mcp)"
    }
  },
  "mcpServers": {
    "memory": {
      "type": "http",
      "url": "${user_config.memory_url}"
    }
  }
}
